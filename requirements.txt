# Production requirements for the unified trading system.
# Optimized for Docker build speed and image size.
# Excludes heavy ML deps (FinBERT) by default — OpenCode is the primary sentiment.

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6

# Data
yfinance>=0.2.31
ccxt>=4.0.0
alpha-vantage>=2.3.1
requests>=2.31.0

# Database
aiosqlite>=0.19.0
sqlalchemy>=2.0.0

# Analysis (Pandas/numpy, no TA-Lib in production for now)
pandas>=2.1.0
numpy>=1.25.0
matplotlib>=3.7.0  # equity-curve / drawdown plots (src/strategy/equity_curve.py)

# Sentiment (OpenCode AI sentiment goes over HTTP via `requests`, above)
vaderSentiment>=3.3.2
feedparser>=6.0.0  # RSS news sentiment source (src/data/sentiment/sources/rss.py)

# Dashboard
pydantic>=2.5.0
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Broker
alpaca-py>=0.21.0

# Utilities
click>=8.1.7

# Unified learning (added 2026-06-30)
lightgbm>=4.0
pyarrow>=14.0
